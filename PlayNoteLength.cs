namespace PlayString
{
    class PlayNoteLength : Command
    {
        public int Length;

        public PlayNoteLength(int length)
        {
            Length = length;
        }
    }
}
